apiVersion: v1
kind: Secret
metadata:
  name: elasticsearch-secret
  namespace: "{{ k8s_namespace }}"
type: Opaque
data:
  AWS_ACCESS_KEY_ID: "{{ aws_access_id | b64encode }}"
  AWS_SECRET_KEY: "{{ aws_secret_key | b64encode }}"
