apiVersion: "v1"
kind: "Service"
metadata:
  name: "elasticsearch"
  namespace: {{ k8s_namespace }}
spec:
  ports:
    - name: "http"
      port: 9200
      targetPort: "http"
    - name: "transport"
      port: 9300
      targetPort: "transport"
  selector:
    app: "elasticsearch"
